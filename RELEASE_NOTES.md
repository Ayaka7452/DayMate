# DayMate v1.16.5

## 优化

- 「保存」按钮移至页面顶部标题右侧，无需再滚动到页面底部寻找
- 目标日期与「还剩 X 天」提示合并为一行，日期块更紧凑
- 「按天数」按钮改为实心突出显示，「重置为今天」改为描边按钮，主次更分明
- 按天数设置支持按「天 / 月 / 年」计数：默认跟随倒计时显示单位，也可手动切换（如显示单位为年，仍可切回按天设置）；快捷值随单位变化（天 7/30/100/365，月 1/3/6/12，年 1/5/10）
- 「所在文件夹」移入「基本信息」分区——事件归属是基本设置，不再收在「更多设置」里
- 「更多设置」分区默认折叠，点击展开；从节日卡片进入或编辑已跟随节日的事件时自动展开
- 「取消跟随」操作内联至跟随节日一行的 ⓘ 图标后，节省纵向空间
- 整页布局压缩，常规屏幕下新建/编辑事件基本无需上下滑动
