# DayMate v1.17.1

## 优化

- 「跟随节日」的说明提示重写：跟随节日选择后，每年将自动倒计时该节日，即使该节日每年可能处于不同的日期——不再绕弯讲锚定机制
- 「对照值」更名为「对照日期」，说明提示同步重写：启用后将在当前倒数天数旁显示对照天数，供事件倒数时对照使用

## 修复

- 「设置 → 数据维护 → 检查数据库」的描述补全：明确检查对象是数据库完整性，结尾补句号（六语言同步）
