# DayMate v1.16.6

## 优化

- 「重置为今天」按钮文案精简为「重置」，按钮内不再折行
- 对照值说明中的「可不填」改为「非必填项」，表述更自然（六语言同步）

## 近期更新（v1.16.5）

- 「保存」按钮移至页面顶部标题右侧；目标日期与「还剩 X 天」合并为一行
- 「按天数」支持按「天 / 月 / 年」计数，默认跟随倒计时单位、可手动切换
- 「所在文件夹」移入「基本信息」；「更多设置」默认折叠，跟随节日场景自动展开
- 整页布局压缩，常规屏幕下新建/编辑事件基本无需上下滑动
