# DayMate v1.17.0

## 修复

- 节日卡片的「还有 X 天 / 今天」与右侧 emoji 的底部对齐彻底解决：不再依赖任何排版模式（框底 / 基线 / 字形框都不可靠），改为绘制软件位图逐像素实测字形真实的墨水边界，按设备当前实际字体（含各家 emoji 字体）自动计算摆放位置，任何机型上底部都精确齐平
- 经过模拟器像素级验证：「今天」分支误差 1px、「还有 X 天」分支误差 0px（均为抗锯齿误差范围内）
